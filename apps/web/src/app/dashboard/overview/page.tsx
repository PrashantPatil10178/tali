// This page is intentionally empty.
// Content is rendered by the parallel route slots (@sales, @bar_stats, etc.)
// and composed in overview/layout.tsx.
export default function OverviewPage() {
  return null;
}
