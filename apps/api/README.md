# API

Bun + Elysia backend app for the Tali monorepo.

## Scripts

- `pnpm dev:api` from the repo root
- `pnpm --filter @tali/api dev` directly
- `pnpm --filter @tali/api build`
- `pnpm --filter @tali/api start`

## Endpoints

- `GET /`
- `GET /health`
